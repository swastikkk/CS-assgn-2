#include <stdio.h>

int main()
{
    int i, n, sum=0;

    printf("Enter the terms upto which you want to find sum: ");
    scanf("%d", &n);

    for(i=1; i<=n; i+=2)
    {
        sum+=i;
    }

    printf("Sum of odd numbers = %d", sum);

    return 0;
}
